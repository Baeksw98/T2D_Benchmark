# Convenience pins; these mirror [project.dependencies] in pyproject.toml.
# `pip install -e .` installs the same set from the package metadata.
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0
statsmodels>=0.14.0
matplotlib>=3.8.0
pytest>=7.4.0
